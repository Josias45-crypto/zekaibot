# ── Base de datos ──
DATABASE_URL=postgresql://techbot_user:TU_PASSWORD_AQUI@localhost:5432/techbot

# ── Redis ──
REDIS_URL=redis://localhost:6379

# ── Seguridad ──
SECRET_KEY=cambia_esto_por_una_cadena_aleatoria_larga
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ── IA (no se usa con Ollama local) ──
ANTHROPIC_API_KEY=no-se-usa
GOOGLE_API_KEY=no-se-usa

# ── App ──
APP_NAME=ZekaiBot
APP_VERSION=1.0.0
DEBUG=True
